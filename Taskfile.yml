version: "3"

tasks:
  run:
    - go run ./main.go -f1 -d, tests/testdata/fourchords.csv

  lint:
    - golangci-lint fmt
    - golangci-lint run --enable-only tagalign,whitespace,nlreturn --fix
    - golangci-lint run

  test:
    - go test ./... -v
